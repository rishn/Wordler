{
	"name": "wordler-nyt-solver-server",
	"version": "1.0.0",
	"private": true,
	"type": "commonjs",
	"engines": {
		"node": ">=18"
	},
	"scripts": {
		"dev": "tsx watch index.ts",
		"build": "tsc -p .",
		"start": "node dist/index.js",
		"postinstall": "npx playwright install --with-deps || npx playwright install"
	},
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^4.19.2",
		"playwright": "^1.49.0"
	},
	"devDependencies": {
		"tsx": "^4.19.0",
		"typescript": "^5.4.0"
	}
}
